<span class='title'><%= link_to TitleTagPresenter.new(prediction.description_with_group).tag, prediction %></span>
<%= render :partial => 'predictions/summary', :locals => {:prediction => prediction} -%>
